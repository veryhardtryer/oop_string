{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "command": "cmake",
      "args": [
        "--build",
        "${workspaceFolder}/build",
        "--config",
        "Debug",
        "--target",
        "string_unit_tests" // Замените на ваше исполняемое имя
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
